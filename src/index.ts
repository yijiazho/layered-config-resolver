/**
 * Public package entry point for the layered config resolver.
 */

/** The package name, exposed for basic runtime and build verification. */
export const PROJECT_NAME = 'layered-config-resolver';
